How to Get the PostgreSQL Database URL
The database URL follows this format:

postgresql://<username>:<password>@<host>:<port>/<database_name>


update model 
npx prisma migrate dev --name add_invite_code_to_doctor


Check if the inviteCode field is now visible in the Doctor table.

npx prisma studio
